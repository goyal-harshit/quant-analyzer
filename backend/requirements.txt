# ── Web Framework ────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# ── Database (open-source, free, self-hosted) ───────────────────
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.3
pydantic-settings==2.5.2   # typed Settings (config.py); required at import time

# ── Data & Analytics (all open-source) ───────────────────────────
pandas==2.2.3
numpy==1.26.4
scipy==1.14.1
httpx[http2]
cachetools==5.5.0

# ── India Market Data — 100% free, open-source, no API keys ──────
yfinance==0.2.44       # Yahoo Finance wrapper, free, open-source
jugaad-data==0.33.1     # Official NSE bhavcopy/historical data, free
nsepython==2.94          # Official NSE live quotes, free
nse[server]               # NseIndiaApi — server-ready (httpx/http2), cookie mgmt, no API key

# ── AI — Ollama (self-hosted, free, open-source LLM) ──────────────
# No client library needed; we call Ollama's local REST API directly
# via httpx (already listed above). Just run `ollama serve` locally.

# ── Background tasks (open-source, free, self-hosted) ─────────────
celery==5.4.0
redis==5.1.1

# ── Auth ────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
python-multipart==0.0.12

# ── Rate limiting ───────────────────────────────────────────────
slowapi==0.1.9

# ── Web scraping (Screener.in fundamentals) ──────────────────────
beautifulsoup4==4.12.3
lxml==5.3.0

# ── File I/O & reporting (Phase C — all open-source) ─────────────
openpyxl==3.1.5         # Excel (.xlsx) read/write for import + export
reportlab==4.2.5        # PDF report generation, BSD-licensed

# ── Utilities ───────────────────────────────────────────────────
python-dotenv==1.0.1
